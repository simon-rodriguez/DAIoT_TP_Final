import { ColorBotonDirective } from './color-boton.directive';

describe('ColorBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBotonDirective();
    expect(directive).toBeTruthy();
  });
});
